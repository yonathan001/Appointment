from django.db import models

# Models for this app (users, services, appointments) are now defined in their respective app directories.
# This api/models.py file can be used for API-specific models if any are needed in the future,
# or can be kept minimal.
